# Minimal Campaign Scaffold Definition
# Standard format for minimal camp CLI campaign initialization
scaffold_version: "1.0.0"
templates_dir: campaign/templates

vars:
  campaign_name: ""
  campaign_id: ""
  campaign_type: "product"

files:
  # .campaign directory structure
  - path: ".campaign/templates/.gitkeep"
    template: "~"
  - path: ".campaign/agents/.gitkeep"
    template: "~"

  # Only projects directory in minimal mode
  - path: "projects/OBEY.md"
    template: "OBEY-projects.md.tmpl"

  # Intents directory (minimal: only inbox)
  - path: "intents/OBEY.md"
    template: "OBEY-intents.md.tmpl"
  - path: "intents/inbox/.gitkeep"
    template: "~"

  # Root AGENTS.md (source of truth, CLAUDE.md symlinks to this)
  - path: "AGENTS.md"
    template: "AGENTS.md.tmpl"
